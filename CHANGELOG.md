# Changelog

记录规则：每次修改都写清楚 `改动`、`原因`、`影响`。

## 2026-04-24

### style: 全局字号下调（空状态与 Agent）
- 改动：
  - 缩小画布空状态的主标题、徽标文案和功能胶囊字号
  - 缩小右侧 Agent 的问候语、快捷卡片、输入框和模式文案字号
- 原因：
  - 当前字号偏大，影响信息密度和视觉平衡
- 影响：
  - 页面阅读压力下降，布局更紧凑，整体观感更协调

### feat: 画布空状态引导层（有节点自动隐藏）
- 改动：
  - 新增画布中心空状态文案与功能胶囊入口（参考图风格）
  - 当画布无节点时显示；创建任意节点后自动隐藏
  - 删除节点至空画布时自动重新显示
- 原因：
  - 需要在初始空画布时提供明确引导，避免页面“空白无提示”
- 影响：
  - 首屏可读性和引导性提升，不影响已有节点编辑流程

### feat: Agent 面板改版 + 右下角小球开关
- 改动：
  - 右侧 Agent 面板视觉改为参考图方向（深色圆角卡片、顶部图标区、底部输入与发送区）
  - 点击 Agent 右上角 `×` 后，面板直接关闭并在右下角显示小球入口
  - 点击右下角小球可重新展开 Agent 面板
  - 左侧工具栏 Agent 按钮与开关状态联动
- 原因：
  - 需要更贴近目标视觉风格，并优化“收起/再打开”的操作路径
- 影响：
  - 右侧区域更简洁，关闭后不占视觉空间，恢复入口更直观

### fix: 缩放中心漂移修复（兼容 CSS zoom 坐标换算）
- 改动：
  - 修正 `screenToCanvas` 在 `CSS zoom` 模式下的坐标映射公式
  - 修正 `zoomAt` 与 `setScaleByPercent` 在 `CSS zoom` 模式下的缩放锚点换算
- 原因：
  - 画布平移后再缩放时，旧公式会导致视野锚点偏移，出现“缩放后目标跑丢”
- 影响：
  - 现在会稳定围绕当前视野位置缩放，定位更可控

### feat: 滚轮交互改为“滚动平移 + Ctrl 缩放（中心锚点）”
- 改动：
  - 普通鼠标滚轮改为画布上下/横向平移（不再直接缩放）
  - `Ctrl + 滚轮` 触发缩放
  - 缩放锚点改为视口中心位置
- 原因：
  - 对齐常见设计工具习惯，避免误缩放并提升滚轮浏览直觉
- 影响：
  - 画布浏览更稳定，缩放操作更可控

### perf: 缩放清晰度修复（优先原生 zoom）
- 改动：
  - 画布缩放渲染改为“优先使用 CSS `zoom`，不支持时回退 `transform scale`”
  - 清理会加重模糊的样式（移除画布 `translateZ(0)` 与 `backface-visibility`）
- 原因：
  - 整层 `transform scale` 在缩放文本/细线时容易出现发虚
- 影响：
  - 在支持 `zoom` 的浏览器中，放大缩小时的文字与边缘清晰度明显提升
  - 旧环境仍保持原有逻辑，不影响兼容性

### style: 节点与输入窗口视觉重塑（质感/立体感增强）
- 改动：
  - 重设深色主题变量与网格背景层次，强化空间纵深
  - 节点卡片、输入面板、上传按钮、连接点改为渐变 + 高光 + 阴影的立体风格
  - 底部模型/比例按钮统一为胶囊化按钮样式，发送按钮改为高亮圆形主操作
  - 优化输入区字体、占位符、边框与面板圆角细节
- 原因：
  - 需要向参考图靠拢，提升窗口质感与视觉完成度
- 影响：
  - 页面整体更接近产品化暗色 UI 风格，节点与控制区层次更清晰

### feat: 生成结果改为同节点就地更新
- 改动：
  - 提示词发送后不再自动创建右侧新节点与自动连线
  - 生成图片直接写回当前节点媒体区域
  - 新增下游参考图刷新：当源节点图片变化时，同步更新已连线目标节点中的对应参考图
- 原因：
  - 当前创作流程需要在同一节点持续迭代，不希望每次生成都扩张画布结构
- 影响：
  - 交互更聚焦，节点数量不再因每次生成自动膨胀
  - 已建立的参考图链路能跟随源节点最新结果更新

### feat: 模型按钮改为下拉列表选择
- 改动：
  - 节点底部模型按钮从“点击循环切换”改为“点击展开下拉列表”
  - 新增模型列表项选中高亮，点击后更新当前节点模型并收起菜单
  - 模型弹层与比例弹层互斥显示，点击空白区域自动收起
- 原因：
  - 后续模型数量会持续增加，循环切换不利于快速精确选择
- 影响：
  - 当前可直接选择 `NanoBanana` / `gpt-image-2`，后续新增模型仅需补配置数组

### feat: 按模型分离 API Key（gpt-image-2 / NanoBanana）
- 改动：
  - 新增 `gpt-image-2` 专用 Key 配置
  - 请求发送时按当前节点模型自动选择对应 `Authorization` Key
- 原因：
  - `gpt-image-2` 与 `NanoBanana` 属于不同密钥体系，需分离鉴权
- 影响：
  - 模型切换后会自动使用对应账号通道，减少鉴权失败

### feat: 节点模型切换支持 gpt-image-2
- 改动：
  - 节点指令区模型按钮支持在 `NanoBanana` 与 `gpt-image-2` 间切换
  - 图生图与文生图请求改为读取当前节点所选模型
- 原因：
  - 需要在节点级别快速切换到 `gpt-image-2`，匹配平台可用模型能力
- 影响：
  - 可直接在你标注位置完成模型切换并生效到实际请求

### feat: 左侧工具栏与顶部布局重构（参考 TapNow 方向）
- 改动：
  - 左侧由单按钮升级为多功能工具栏（添加/居中/重置/Agent）
  - 顶部加入操作胶囊区，优化页面信息密度
  - 节点菜单位置与层级调整，提升点击命中
- 原因：
  - 原页面结构单薄，入口不清晰，存在“看起来能点但不好用”的体验问题
- 影响：
  - 主操作入口更明确，画布交互效率提升，界面更接近目标视觉风格

### feat: 工具栏提示气泡与激活态反馈
- 改动：
  - 左侧按钮新增 hover 提示气泡
  - 菜单开关与 Agent 显示状态支持高亮联动
- 原因：
  - 需要降低新用户学习成本，并提升操作状态可见性
- 影响：
  - 交互反馈更直接，误操作成本降低

### perf: 缩放清晰度优化
- 改动：
  - 画布 `translate` 对齐设备像素，`scale` 做精度收敛
  - 补充缩放渲染优化样式，缓解放大模糊
- 原因：
  - 用户反馈放大后画面模糊，影响阅读与编辑体验
- 影响：
  - 缩放过程更稳定，清晰度表现改善

### chore: 模型参数更新
- 改动：
  - 前端请求模型统一为 `gemini-3.1-flash-image-preview-4k`
- 原因：
  - API 平台模型命名更新，需要同步前端参数
- 影响：
  - 前端请求参数与平台新模型名一致
  - 若仍报“无可用通道”，需在平台分组后台开通对应模型渠道

## 2026-04-23

### feat: 画布页面外层 UI 扩展（左上/右侧/左下）
- 改动：
  - 新增左上品牌区（Logo + 标题）
  - 新增右侧 Agent 面板（问候、快捷动作、输入区、收起/展开）
  - 新增左下画布控制条（缩放滑杆、放大/缩小、重置、百分比）
  - 将缩放控件接入现有画布缩放状态同步
  - 增加滚轮事件区域隔离，避免在侧栏和控制条内误触发画布缩放
- 原因：
  - 当前页面信息密度偏低，仅左侧单按钮，整体观感与可操作性不足
  - 需要向参考资料视觉结构靠拢，提升产品完成度
- 影响：
  - 首页更接近完整产品形态，操作入口更清晰
  - 后续可在 Agent 与工具条上继续扩展功能而不影响画布核心逻辑

### docs: add project README (`cbb5c2a`)
- 改动：
  - 新增 `README.md`
  - 补充项目简介、功能概览、目录结构、运行方式、Git 工作流
- 原因：
  - 需要让项目具备基础可读性与可交接性，便于后续继续开发
- 影响：
  - 新成员或新 AI 可快速理解项目上下文

### chore: initial commit for infinite canvas project (`3e77cb1`)
- 改动：
  - 初始化仓库并提交现有项目文件
  - 包含 `index.html` / `style.css` / `script.js` / `项目说明.md` / `参考资料`
- 原因：
  - 需要建立可追踪的版本基线，并同步到 GitHub
- 影响：
  - 项目进入可持续迭代状态（可回滚、可协作、可交接）

### feat: 参考图流转能力补全（已入当前代码基线）
- 改动：
  - 连线后自动传递上游媒体到下游参考图区
  - 参考图缩略图渲染、手动添加、删除
  - 生成请求优先使用参考图集合作为图生图输入
- 原因：
  - 实现节点式工作流里最关键的 Img2Img 条件继承能力
- 影响：
  - 多节点链路可形成连续视觉推演

### feat: 交互流畅度优化（已入当前代码基线）
- 改动：
  - 画布缩放改为鼠标锚点缩放
  - 平移加入惯性
  - 重绘改为 `requestAnimationFrame` 调度
  - 细节动效增强（按钮/连接点/节点拖拽状态）
- 原因：
  - 提升无限画布操作手感，贴近 TapNow 风格
- 影响：
  - 拖拽、缩放、连线时更丝滑，视觉跳变减少

### refactor: 三文件职责拆分（已入当前代码基线）
- 改动：
  - `index.html` 保持结构层
  - `style.css` 承载样式和动效
  - `script.js` 承载交互逻辑
  - 清理逻辑模板中的内联 `style`，改为 CSS class
- 原因：
  - 避免代码糅杂，提升可维护性和后续协作效率
- 影响：
  - 代码分层清晰，后续改动风险更低
