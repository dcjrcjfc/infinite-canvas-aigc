# Changelog

记录规则：每次修改都写清楚 `改动`、`原因`、`影响`。

## 2026-04-26

### feat: 历史与模型策略调整（生成历史单通道 + 多模型分层）
- 改动：
  - 历史面板调整为仅记录“生成”结果（过滤旧记录并停止写入非生成类历史）
  - 图片/文本节点模型增加 `Midjourney`、`即梦AI`
  - 视频节点模型切换为 `Seedance`、`可灵`（移除 `NanoBanana` / `gpt-image-2`）
  - Agent 底部 `Pro` 改为 LLM 选择器（Gemini / ChatGPT / DeepSeek）
  - 代理模型路由补充新增模型 ID 映射
- 原因：
  - 按最新产品要求收敛历史信息粒度，并按内容类型拆分模型入口
- 影响：
  - 历史面板信息更聚焦，默认只展示可回看的生成结果
  - 节点模型选择更贴合图片/视频任务场景，Agent 模型选择可视化

### feat: 模板化创作入口 + TapNow 风格收口首阶段
- 改动：
  - 空画布新增模板卡片入口（角色设定流 / 分镜草案流 / 海报概念流）
  - 新增模板一键建链路能力：自动创建节点、自动连线、填充默认提示词
  - 模板创建动作接入历史记录与 `Undo / Redo`
  - 顶部胶囊改为可执行模板快捷入口（角色设定流 / 分镜草案流）
  - 空状态与顶部胶囊样式细节收口（层级、hover/active、响应式）
- 原因：
  - 需要把“模板化创作入口”和“TapNow 风格细节统一”从规划项落地为可直接使用的能力
- 影响：
  - 新用户可在空画布一键进入可编辑工作流，首屏上手成本下降
  - 视觉反馈一致性提升，页面在桌面/窄屏场景更稳定

### feat: 交互体验收口批（Agent 状态反馈 + 生成重试与分级提示）
- 改动：
  - Agent 面板新增状态位（就绪/处理中/成功/警告/错误）
  - Agent 快捷动作新增处理中态，输入框支持 `Ctrl+Enter` 快速发送
  - 全局 toast 增加 `info/success/warn/error` 分级样式
  - 生成请求新增自动重试（覆盖网络波动、429、5xx 等可重试场景）
  - `runAI` 失败改为分级提示，节点状态在“生成中/失败回退/成功”间正确切换
- 原因：
  - 上一阶段核心能力已具备，但交互反馈与请求容错仍偏薄弱，影响连续创作体验
- 影响：
  - 用户可明确感知 Agent 与生成链路的当前状态
  - 临时网络波动下成功率提升，失败信息更可操作

### feat: 安全整改首阶段（后端代理 + 前端去密钥）
- 改动：
  - 新增 `proxy-server.js`，提供同源 `/api/images/generations` 与 `/api/images/edits` 代理
  - 前端请求改为走代理，不再直连上游地址
  - 前端移除明文 `API Key` 与 `Authorization` 拼接逻辑
  - 新增 `.env.example` 与 `.gitignore` 环境变量忽略规则
  - 更新 `README.md`、`PROJECT_STATUS.md` 的运行与安全说明
- 原因：
  - 前端明文密钥存在高泄露风险，需满足“上线前必须完成”的安全要求
- 影响：
  - 密钥不再暴露在浏览器侧，基础安全性显著提升
  - 本地运行方式从“直接打开 HTML”升级为“启动代理服务后访问本地地址”

### feat: 参考图工作流增强（主参考 + 权重 + 顺序 + 来源可视化）
- 改动：
  - 参考图区升级为卡片化结构，支持每张参考图独立操作
  - 新增主参考图标记（可切换主参考）
  - 新增参考图权重调节（0.5x~2x）
  - 新增参考图顺序调整（左右移动）
  - 新增来源可视化（手动上传 / 链路来源节点）并支持一键定位到来源节点
  - 连线流转与节点重命名时会同步刷新参考图来源信息
- 原因：
  - 需要将参考图从“仅展示”升级为“可编排、可追踪”的生产级工作流
- 影响：
  - 参考图控制粒度显著提升，图生图输入组织更可控
  - 链路来源更透明，排错与回看效率提升

### feat: 下一批节点系统增强（重命名 + 复制粘贴 + 批量选择）
- 改动：
  - 新增节点头部元信息（类型图标 + 状态标签），并接入状态更新
  - 新增节点重命名（双击节点名或 `F2`）
  - 新增节点复制/粘贴（`Ctrl+C` / `Ctrl+V`），支持多节点与内部连线一起粘贴
  - 新增批量选择能力（`Shift + 点击` 多选、`Shift + 拖拽` 框选）
  - 新增多节点拖拽与批量删除，并接入现有 Undo/Redo 命令栈
- 原因：
  - 按下一批计划补齐节点级高频操作，提升复杂画布编辑效率
- 影响：
  - 节点管理从“单点操作”升级为“批量操作”，显著降低重复编辑成本
  - 节点可读性增强（类型/状态可视化），协作与回看更直观

### feat: 第三批落地（Undo/Redo + 历史筛选回放）
- 改动：
  - 新增场景快照命令栈，支持 `Undo / Redo`
  - 撤销/重做覆盖节点增删、连线增删、图片编辑参数变更
  - 历史面板新增类型筛选（全部/节点/连线/编辑/上传/生成/Agent）
  - 历史项新增“定位/回放”动作，支持快速回放并还原历史状态
  - 历史面板新增“撤销/重做”按钮，并接入快捷键 `Ctrl+Z` / `Ctrl+Shift+Z` / `Ctrl+Y`
- 原因：
  - 按第三批计划将编辑与历史能力从“可查看”升级为“可回退、可还原”的生产级能力
- 影响：
  - 关键编辑链路具备容错能力，误操作可撤销
  - 历史可按类型快速检索并一键回放，追溯效率明显提升

### docs: 里程碑同步与第三批落地计划
- 改动：
  - 更新 `PROJECT_STATUS.md` 为 2026-04-26 最新进度
  - 明确当前阶段已完成项与“第三批落地”优先级计划
- 原因：
  - 当前迭代跨度较大，需要统一状态口径并收敛下一步执行顺序
- 影响：
  - 项目推进路径更清晰，后续开发可按批次稳定落地

### feat: 第一批“可落地功能”补全（非静态按钮）
- 改动：
  - `历史` 面板改为真实记录（本地存储、渲染、清空）
  - `图片编辑器` 面板支持对图片节点应用亮度/对比度/饱和度
  - Agent 快捷动作可写入输入内容，Agent 发送可落到节点或创建文本节点
  - 顶部三按钮改为可执行动作（提示、居中并打开菜单、打开仓库）
  - 新增 toast 反馈，所有关键动作给出即时结果提示
- 原因：
  - 解决“页面能看但点不动”的问题，把核心入口变成可执行功能
- 影响：
  - 交互闭环增强，当前版本可用于真实创作流程而非纯展示

### feat: 第二批可用性补全（历史定位 + 编辑增强 + 个人菜单动作）
- 改动：
  - 历史记录项支持点击定位到对应节点（自动选中并居中视图）
  - 图片编辑器新增旋转90°与镜像，并和亮度/对比度/饱和度共同生效
  - 个人信息菜单项接入实际动作（通知提示、主页跳转、账户/教程/登出反馈）
  - Agent 顶部操作按钮接入“新建文本节点”“打开历史”
- 原因：
  - 继续消除“可点不可用”入口，补齐常用动作的执行闭环
- 影响：
  - 使用路径更完整，历史可追踪可回看，编辑能力从基础调整提升到实用层

### feat: 双击画布弹出节点菜单 + 空状态文案再缩小
- 改动：
  - 新增画布双击事件：双击空白画布可直接弹出“添加节点/上传”菜单
  - 菜单会根据双击位置智能定位，避免超出视口
  - 空状态引导区（标题/徽标/胶囊）字号再缩小一档
  - 空状态引导与菜单显示联动：双击弹菜单时引导立即隐藏，菜单关闭且无节点时恢复显示
- 原因：
  - 需要更符合“双击即可开始”的交互预期，并降低空状态视觉占比
- 影响：
  - 首次创建节点更直接，空状态信息更克制，交互状态更清晰

### feat: 左侧快捷区改为历史 / 图片编辑器 / 个人信息
- 改动：
  - 移除左侧重复功能入口，替换为“历史”“图片编辑器”“个人信息”三个按钮
  - 新增历史与图片编辑器弹层（占位信息）
  - 新增个人信息菜单（头像、邮箱、通知/主页/账户/教程/登出项）
  - 增加弹层开关与外部点击自动关闭逻辑
- 原因：
  - 左侧入口存在重复，需调整为更高频的产品功能入口
- 影响：
  - 左侧工具区信息架构更清晰，后续可直接扩展历史与编辑器能力

### feat: 菜单建节点时自动连线并继承参考图
- 改动：
  - 从已选节点或连线起点通过菜单新建节点时，自动创建连线到新节点
  - 自动把上游节点图片写入下游节点参考图区
  - 连线创建逻辑抽为通用方法，菜单建节点与手动连线复用
- 原因：
  - 需要实现“上传图 -> 点加号选功能 -> 下游节点自动带参考图”的工作流
- 影响：
  - 多步图像工作流更顺滑，减少手动重复操作

### feat: 上传节点轻量化 + 加号短按/长按双模式
- 改动：
  - 上传生成的图片节点改为轻量结构：仅图片框 + 左右加号（不含提示词输入区）
  - 加号短按：弹出功能菜单
  - 加号长按：进入连线模式并显示临时连线
  - 菜单支持记住触发连接点，创建节点后自动按方向连线
- 原因：
  - 需要区分“素材节点”和“生成节点”，并把加号交互统一为更高效的单入口
- 影响：
  - 素材节点更干净，连线与功能选择更顺手，操作路径更符合目标交互

### style: 移除空状态功能胶囊文案
- 改动：
  - 删除空画布引导区第二行功能胶囊文字（红框区域）
- 原因：
  - 需要简化空状态视觉信息，只保留核心“双击”提示
- 影响：
  - 首屏更简洁，视觉聚焦更明确

### feat: 顶部三按钮随 Agent 开关自动换位
- 改动：
  - 新增 `body.agent-open / body.agent-closed` 状态类
  - Agent 打开时顶部三按钮保持原位置
  - Agent 关闭时顶部三按钮平滑移动到页面右上角
- 原因：
  - 关闭 Agent 后顶部空间释放，需要同步提升操作入口可达性
- 影响：
  - 布局响应 Agent 状态更自然，顶部操作区利用率更高

### style: 连接点与节点分离 + 连接点灵动动效
- 改动：
  - 左右 `+` 连接点外移，与节点框体拉开明显间距
  - 为连接点增加轻微浮动/呼吸动画与增强 hover 反馈
  - 连线激活态保留高亮并停止浮动，保证状态清晰
- 原因：
  - 原连接点与框体过近，视觉拥挤，不符合目标风格
- 影响：
  - 连接入口更醒目，交互感更灵动，节点边缘更干净

### style: TapNow 风格 3D 质感 + 鼠标靠近动态
- 改动：
  - 为节点、顶部按钮、左侧工具、底部控制和 Agent 区域增加 3D 质感样式（透视、浮层高光、阴影）
  - 新增鼠标靠近动态：根据鼠标与元素距离，进行轻微倾斜、位移和缩放
  - 增加窗口失焦时的动态重置，避免界面停留在倾斜状态
- 原因：
  - 当前 UI 偏扁平，和目标 TapNow 风格的“立体 + 呼吸感”存在差距
- 影响：
  - 视觉层次更强，交互反馈更灵动，整体风格更接近目标方向

## 2026-04-25

### docs: README 全量重写与细化
- 改动：
  - 重写 `README.md`，补充项目目标、能力清单、架构分层、交互速查、API 配置、风险与路线图
  - 增加对当前模型选择、滚轮交互、空状态、Agent 开关等现状说明
- 原因：
  - 原 README 信息量不足，难以支持新成员快速理解与接手
- 影响：
  - 项目可读性和交接效率显著提升，后续维护成本降低

## 2026-04-24

### style: 全局字号下调（空状态与 Agent）
- 改动：
  - 缩小画布空状态的主标题、徽标文案和功能胶囊字号
  - 缩小右侧 Agent 的问候语、快捷卡片、输入框和模式文案字号
- 原因：
  - 当前字号偏大，影响信息密度和视觉平衡
- 影响：
  - 页面阅读压力下降，布局更紧凑，整体观感更协调

### feat: 画布空状态引导层（有节点自动隐藏）
- 改动：
  - 新增画布中心空状态文案与功能胶囊入口（参考图风格）
  - 当画布无节点时显示；创建任意节点后自动隐藏
  - 删除节点至空画布时自动重新显示
- 原因：
  - 需要在初始空画布时提供明确引导，避免页面“空白无提示”
- 影响：
  - 首屏可读性和引导性提升，不影响已有节点编辑流程

### feat: Agent 面板改版 + 右下角小球开关
- 改动：
  - 右侧 Agent 面板视觉改为参考图方向（深色圆角卡片、顶部图标区、底部输入与发送区）
  - 点击 Agent 右上角 `×` 后，面板直接关闭并在右下角显示小球入口
  - 点击右下角小球可重新展开 Agent 面板
  - 左侧工具栏 Agent 按钮与开关状态联动
- 原因：
  - 需要更贴近目标视觉风格，并优化“收起/再打开”的操作路径
- 影响：
  - 右侧区域更简洁，关闭后不占视觉空间，恢复入口更直观

### fix: 缩放中心漂移修复（兼容 CSS zoom 坐标换算）
- 改动：
  - 修正 `screenToCanvas` 在 `CSS zoom` 模式下的坐标映射公式
  - 修正 `zoomAt` 与 `setScaleByPercent` 在 `CSS zoom` 模式下的缩放锚点换算
- 原因：
  - 画布平移后再缩放时，旧公式会导致视野锚点偏移，出现“缩放后目标跑丢”
- 影响：
  - 现在会稳定围绕当前视野位置缩放，定位更可控

### feat: 滚轮交互改为“滚动平移 + Ctrl 缩放（中心锚点）”
- 改动：
  - 普通鼠标滚轮改为画布上下/横向平移（不再直接缩放）
  - `Ctrl + 滚轮` 触发缩放
  - 缩放锚点改为视口中心位置
- 原因：
  - 对齐常见设计工具习惯，避免误缩放并提升滚轮浏览直觉
- 影响：
  - 画布浏览更稳定，缩放操作更可控

### perf: 缩放清晰度修复（优先原生 zoom）
- 改动：
  - 画布缩放渲染改为“优先使用 CSS `zoom`，不支持时回退 `transform scale`”
  - 清理会加重模糊的样式（移除画布 `translateZ(0)` 与 `backface-visibility`）
- 原因：
  - 整层 `transform scale` 在缩放文本/细线时容易出现发虚
- 影响：
  - 在支持 `zoom` 的浏览器中，放大缩小时的文字与边缘清晰度明显提升
  - 旧环境仍保持原有逻辑，不影响兼容性

### style: 节点与输入窗口视觉重塑（质感/立体感增强）
- 改动：
  - 重设深色主题变量与网格背景层次，强化空间纵深
  - 节点卡片、输入面板、上传按钮、连接点改为渐变 + 高光 + 阴影的立体风格
  - 底部模型/比例按钮统一为胶囊化按钮样式，发送按钮改为高亮圆形主操作
  - 优化输入区字体、占位符、边框与面板圆角细节
- 原因：
  - 需要向参考图靠拢，提升窗口质感与视觉完成度
- 影响：
  - 页面整体更接近产品化暗色 UI 风格，节点与控制区层次更清晰

### feat: 生成结果改为同节点就地更新
- 改动：
  - 提示词发送后不再自动创建右侧新节点与自动连线
  - 生成图片直接写回当前节点媒体区域
  - 新增下游参考图刷新：当源节点图片变化时，同步更新已连线目标节点中的对应参考图
- 原因：
  - 当前创作流程需要在同一节点持续迭代，不希望每次生成都扩张画布结构
- 影响：
  - 交互更聚焦，节点数量不再因每次生成自动膨胀
  - 已建立的参考图链路能跟随源节点最新结果更新

### feat: 模型按钮改为下拉列表选择
- 改动：
  - 节点底部模型按钮从“点击循环切换”改为“点击展开下拉列表”
  - 新增模型列表项选中高亮，点击后更新当前节点模型并收起菜单
  - 模型弹层与比例弹层互斥显示，点击空白区域自动收起
- 原因：
  - 后续模型数量会持续增加，循环切换不利于快速精确选择
- 影响：
  - 当前可直接选择 `NanoBanana` / `gpt-image-2`，后续新增模型仅需补配置数组

### feat: 按模型分离 API Key（gpt-image-2 / NanoBanana）
- 改动：
  - 新增 `gpt-image-2` 专用 Key 配置
  - 请求发送时按当前节点模型自动选择对应 `Authorization` Key
- 原因：
  - `gpt-image-2` 与 `NanoBanana` 属于不同密钥体系，需分离鉴权
- 影响：
  - 模型切换后会自动使用对应账号通道，减少鉴权失败

### feat: 节点模型切换支持 gpt-image-2
- 改动：
  - 节点指令区模型按钮支持在 `NanoBanana` 与 `gpt-image-2` 间切换
  - 图生图与文生图请求改为读取当前节点所选模型
- 原因：
  - 需要在节点级别快速切换到 `gpt-image-2`，匹配平台可用模型能力
- 影响：
  - 可直接在你标注位置完成模型切换并生效到实际请求

### feat: 左侧工具栏与顶部布局重构（参考 TapNow 方向）
- 改动：
  - 左侧由单按钮升级为多功能工具栏（添加/居中/重置/Agent）
  - 顶部加入操作胶囊区，优化页面信息密度
  - 节点菜单位置与层级调整，提升点击命中
- 原因：
  - 原页面结构单薄，入口不清晰，存在“看起来能点但不好用”的体验问题
- 影响：
  - 主操作入口更明确，画布交互效率提升，界面更接近目标视觉风格

### feat: 工具栏提示气泡与激活态反馈
- 改动：
  - 左侧按钮新增 hover 提示气泡
  - 菜单开关与 Agent 显示状态支持高亮联动
- 原因：
  - 需要降低新用户学习成本，并提升操作状态可见性
- 影响：
  - 交互反馈更直接，误操作成本降低

### perf: 缩放清晰度优化
- 改动：
  - 画布 `translate` 对齐设备像素，`scale` 做精度收敛
  - 补充缩放渲染优化样式，缓解放大模糊
- 原因：
  - 用户反馈放大后画面模糊，影响阅读与编辑体验
- 影响：
  - 缩放过程更稳定，清晰度表现改善

### chore: 模型参数更新
- 改动：
  - 前端请求模型统一为 `gemini-3.1-flash-image-preview-4k`
- 原因：
  - API 平台模型命名更新，需要同步前端参数
- 影响：
  - 前端请求参数与平台新模型名一致
  - 若仍报“无可用通道”，需在平台分组后台开通对应模型渠道

## 2026-04-23

### feat: 画布页面外层 UI 扩展（左上/右侧/左下）
- 改动：
  - 新增左上品牌区（Logo + 标题）
  - 新增右侧 Agent 面板（问候、快捷动作、输入区、收起/展开）
  - 新增左下画布控制条（缩放滑杆、放大/缩小、重置、百分比）
  - 将缩放控件接入现有画布缩放状态同步
  - 增加滚轮事件区域隔离，避免在侧栏和控制条内误触发画布缩放
- 原因：
  - 当前页面信息密度偏低，仅左侧单按钮，整体观感与可操作性不足
  - 需要向参考资料视觉结构靠拢，提升产品完成度
- 影响：
  - 首页更接近完整产品形态，操作入口更清晰
  - 后续可在 Agent 与工具条上继续扩展功能而不影响画布核心逻辑

### docs: add project README (`cbb5c2a`)
- 改动：
  - 新增 `README.md`
  - 补充项目简介、功能概览、目录结构、运行方式、Git 工作流
- 原因：
  - 需要让项目具备基础可读性与可交接性，便于后续继续开发
- 影响：
  - 新成员或新 AI 可快速理解项目上下文

### chore: initial commit for infinite canvas project (`3e77cb1`)
- 改动：
  - 初始化仓库并提交现有项目文件
  - 包含 `index.html` / `style.css` / `script.js` / `项目说明.md` / `参考资料`
- 原因：
  - 需要建立可追踪的版本基线，并同步到 GitHub
- 影响：
  - 项目进入可持续迭代状态（可回滚、可协作、可交接）

### feat: 参考图流转能力补全（已入当前代码基线）
- 改动：
  - 连线后自动传递上游媒体到下游参考图区
  - 参考图缩略图渲染、手动添加、删除
  - 生成请求优先使用参考图集合作为图生图输入
- 原因：
  - 实现节点式工作流里最关键的 Img2Img 条件继承能力
- 影响：
  - 多节点链路可形成连续视觉推演

### feat: 交互流畅度优化（已入当前代码基线）
- 改动：
  - 画布缩放改为鼠标锚点缩放
  - 平移加入惯性
  - 重绘改为 `requestAnimationFrame` 调度
  - 细节动效增强（按钮/连接点/节点拖拽状态）
- 原因：
  - 提升无限画布操作手感，贴近 TapNow 风格
- 影响：
  - 拖拽、缩放、连线时更丝滑，视觉跳变减少

### refactor: 三文件职责拆分（已入当前代码基线）
- 改动：
  - `index.html` 保持结构层
  - `style.css` 承载样式和动效
  - `script.js` 承载交互逻辑
  - 清理逻辑模板中的内联 `style`，改为 CSS class
- 原因：
  - 避免代码糅杂，提升可维护性和后续协作效率
- 影响：
  - 代码分层清晰，后续改动风险更低
