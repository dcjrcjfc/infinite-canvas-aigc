# 🚀 AIGC 节点式无限画布推演系统 (AIGC Infinite Canvas Design Workflow)

## 1. 项目简介
本项目是一个基于 Web 原生技术构建的**节点式无限画布设计工作流系统**。它打破了传统设计软件线性、固定的画板限制，允许用户在一个可无限缩放和平移的二维坐标系中，自由发散思维。

系统深度集成了 NanoBanana AIGC 图像生成大模型，通过可视化的节点连线，实现了“指令输入 - 图像生成 - 局部重绘/风格转移 - 多方案并行对比”的闭环工作流。它不仅是一个 AI 绘画工具，更是一个帮助设计师梳理复杂空间逻辑、进行视觉方案推演的数字化实验平台。

## 2. 核心应用场景
该系统特别适合处理需要高度整合与全局视野的复杂设计任务：

* **设计方法学实验与评估：** 平台清晰的节点流转记录，非常适合作为对照实验的测试工具。可用于量化对比“AIGC 辅助节点设计”与“传统线性设计”在出图效率和方案质量上的差异，方便后续导入专家打分体系（Expert Scoring）进行效度验证。
* **复杂空间与室内设计推演：** 针对具有特定功能需求的空间（如办公空间、适老化环境设计等），可以在画布上平铺实地勘测图、CAD 线稿，并通过节点连线实时渲染出不同材质和光影的意向效果图，构建全局灵感板（Mood Board）。
* **多分支方案对比：** 针对同一张底图或线稿，可以引出多条连线，分别输入不同的 Prompt 或画幅比例，在同一视野下直观对比多种设计可能性。

## 3. 核心功能特性
* **🌌 纯前端无限画布引擎：**
  * 实现了基于虚拟 DOM 和 CSS `transform` 的丝滑平移（Pan）与无极缩放（Zoom）。
  * 完善的世界坐标系与视口坐标系转换逻辑。
* **🔗 动态节点连线与状态追踪：**
  * 采用 SVG 动态绘制贝塞尔曲线，实时跟随节点拖拽更新。
  * 具备完善的节点增删、连线销毁等内存管理机制。
* **🧬 视觉上下文继承（参考图流转）：**
  * 核心亮点功能。当节点被连线打通时，前置节点的图像（包括本地上传图和 AI 生成图）会自动作为“参考图”传递给下一个节点，完美模拟专业 AIGC 流程中的图生图（Img2Img）条件输入。
* **⚡ 模块化 AIGC 接口对接：**
  * 智能区分“文生图”与“图生图”模式，自动封装 `FormData` 或 `JSON` 载荷。
  * 内置乐观 UI（Optimistic UI）交互，请求发出即刻生成带有“呼吸灯”动画的占位节点，提供极佳的等待体验。

## 4. 技术栈说明
本项目追求极简与高运行效率，**零外部依赖**（不使用 React/Vue 等框架，不依赖 Webpack/Vite 等构建工具），开箱即用：

* **核心结构：** `HTML5` (语义化 DOM 结构，Canvas 视口容器)
* **视觉呈现：** `CSS3` (CSS 变量，Flex/Grid 布局，毛玻璃效果，关键帧动画)
* **交互与逻辑：** `Vanilla JavaScript (ES6+)` (事件代理，坐标系映射，Fetch API，DOM 动态渲染)
* **连线渲染：** `SVG` (可缩放矢量图形 `<path>` 动态生成)

## 5. 项目文件结构
为保证代码的可维护性与扩展性，项目已拆分为经典的前端三剑客结构：

```text
📁 无限画布/
│
├── 📄 index.html      # 主入口文件（定义画布视口、UI 菜单面板、引入 CSS 和 JS）
├── 📄 style.css       # 样式表（控制夜间模式主题、无限网格背景、节点动画及参考图 UI）
└── 📄 script.js       # 核心逻辑引擎（处理 API 请求、鼠标拖拽映射、连线算法与节点数据通信）