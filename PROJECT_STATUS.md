# Project Status

最后更新：2026-04-26

## 1. 当前进度

### 已完成
- 项目已上传 GitHub：`main` 分支可用
- 三文件职责已明确并落实：
  - `无限画布/index.html`：仅结构与资源引用
  - `无限画布/style.css`：仅视觉样式与动效
  - `无限画布/script.js`：仅交互和业务逻辑
- 已实现核心功能：
  - 无限画布平移、缩放、节点拖拽
  - SVG 连线与节点删除联动清理
  - 文生图/图生图请求
  - 参考图自动流转（连线后上游图传递下游）
  - 参考图手动添加与移除
- 已做一轮交互流畅度优化：
  - 鼠标锚点缩放
  - 平移惯性
  - 基于 `requestAnimationFrame` 的重绘调度
  - 节点/按钮/连线细节动效
- 已完成一轮画布外层 UI 丰富化（参考资料视觉方向）：
  - 左上品牌 Logo 区
  - 右侧 Agent 助手侧栏（支持收起/展开）
  - 左下画布控制条（缩放滑杆、放大/缩小、重置、百分比）
  - 画布滚轮在侧栏与控制条区域做了防误触处理
- 已完成左侧工具栏升级与可用性修复：
  - 左侧由单按钮升级为多功能工具栏（添加/居中/重置/Agent）
  - 按钮支持提示气泡与激活态反馈
  - 顶部加入操作胶囊区，整体布局更接近参考风格
- 已完成缩放清晰度优化：
  - 画布位移对齐设备像素，缩放比例做精度收敛
  - 缩放渲染优化，降低放大时的模糊感
- 已完成模型切换能力升级：
  - 节点模型从“单一按钮”升级为“下拉列表可选”
  - 图片/文本节点模型：`NanoBanana` / `gpt-image-2` / `Midjourney` / `即梦AI`
  - 视频节点模型：`Seedance` / `可灵`
  - 不同模型支持使用不同 API Key 路由
- 已完成生成流程调整：
  - 生成结果改为同节点就地更新（不再自动新建结果节点）
  - 上游节点更新后可同步刷新下游参考图
- 已完成滚轮/缩放交互修正：
  - 普通滚轮改为画布平移，`Ctrl + 滚轮` 才缩放
  - 修复缩放中心漂移，避免“放大后目标跑丢”
- 已完成右侧 Agent 交互改版：
  - 面板风格升级
  - 点击右上角关闭后，右下角显示小球入口；点击小球可恢复面板
- 已完成空画布引导层：
  - 无节点时显示中间引导内容
  - 有节点时自动隐藏；删空后重新显示
- 已完成一轮字号收敛：
  - 缩小空状态与 Agent 区域文字，提升信息密度与视觉平衡
- 已完成加号交互重构：
  - 上传素材节点改为轻量结构（仅图片框 + 左右加号）
  - 加号短按弹菜单、长按进入连线模式
  - 菜单建节点支持自动连线并继承上游参考图
- 已完成第一批可落地功能补全：
  - 历史面板支持本地存储/渲染/清空
  - 图片编辑器支持亮度/对比度/饱和度应用
  - Agent 快捷动作与发送可落到节点或创建文本节点
  - 顶部三按钮接入实际动作
  - 全局 toast 反馈接入关键动作
- 已完成第二批可落地功能补全：
  - 历史记录项可点击定位到对应节点（自动居中 + 选中）
  - 图片编辑器新增旋转90°与镜像，并可与滤镜叠加
  - 个人信息菜单项接入实际动作（通知/主页/账户/教程/登出反馈）
  - Agent 顶部按钮接入“新建文本节点”“打开历史”
- 已完成第三批关键落地（编辑与历史生产级首阶段）：
  - 新增 `Undo / Redo`，覆盖节点增删、连线增删、图片编辑参数变更
  - 历史支持快速回放（定位 + 状态还原）
  - 历史策略调整为仅记录“生成”结果（不记录节点/连线/编辑等操作）
- 已完成下一批节点系统增强（首阶段）：
  - 节点重命名（双击节点名 / `F2`）
  - 节点复制/粘贴（`Ctrl+C` / `Ctrl+V`，支持多节点 + 内部连线）
  - 批量选择（`Shift + 点击` / `Shift + 拖拽框选`）
  - 节点类型图标与状态标签接入（素材/生成/已上传/已生成）
- 已完成参考图工作流增强（首阶段）：
  - 主参考图标记（单节点内可切换主参考）
  - 参考图权重控制（0.5x~2x）
  - 参考图顺序调整（左右移动）
  - 引用来源可视化（手动上传 / 链路来源）并支持来源定位
- 已完成安全整改（首阶段）：
  - 新增 `proxy-server.js` 后端代理，前端改为同源 `/api/images/*` 调用
  - API Key 下线到后端环境变量（`NANOBANANA_API_KEY` / `GPT_IMAGE_2_API_KEY`）
  - 前端移除明文密钥与直连 `Authorization` 逻辑
- 已完成交互体验收口批（Agent + 生成反馈）：
  - Agent 面板新增状态标识（就绪/处理中/成功/警告/错误）
  - Agent 快捷动作新增处理中反馈，输入框支持 `Ctrl+Enter` 快速发送
  - Agent 底部模型从固定 `Pro` 改为 LLM 可选（Gemini / ChatGPT / DeepSeek）
  - 全局 toast 新增信息分级样式（info/success/warn/error）
  - 生成请求新增自动重试（网络/限流/5xx），并接入分级错误提示
  - 生成时节点状态改为“生成中”，失败时自动回退状态
- 已完成模板化创作入口（首阶段）：
  - 空画布引导区新增模板入口（角色设定流 / 分镜草案流 / 海报概念流）
  - 模板支持一键创建节点骨架、自动连线、默认提示词填充
  - 模板创建动作接入历史记录与 `Undo / Redo`
- 已完成 TapNow 风格收口（首阶段）：
  - 空状态新增副文案与模板卡片区，视觉层级更清晰
  - 顶部胶囊与按钮 hover/active 反馈统一
  - 桌面/窄屏场景的空状态布局响应式适配

### 进行中
- UI 细节第二阶段仍在打磨（目标参考 TapNow / 当前参考图风格）

### 阻塞/风险
- 网络偶发不稳定会影响远程推送与接口请求
- 代理服务仍需生产化加固（鉴权、限流、审计日志、部署隔离）
- API 平台分组渠道需与模型权限一致，否则会报“无可用通道”

## 2. 下一步（优先级）

1. 模板扩展与可配置化
   - 增加更多模板流（例如封面图、KV 变体、角色表情包）
   - 支持模板参数编辑（主题/比例/节点数量）后再落地
2. 真实 API 联调回归
   - 在代理真实环境下验证文生图/图生图成功率与重试路径
   - 补充失败场景回归（鉴权失败、限流、5xx、网络中断）
3. 代理服务生产化（安全加固）
   - 增加鉴权校验、限流与日志脱敏
   - 为生产部署补充环境隔离与密钥轮换流程

## 3. 开发约束（必须遵守）

- `index.html` 只写 DOM 结构，不写内联 CSS/JS
- `style.css` 只写样式，不写行为逻辑
- `script.js` 只写逻辑，不写内联样式字符串
- 每次改动都需要更新 `CHANGELOG.md`（包含“原因”）

## 4. 交接给新 AI 的启动信息

- 仓库：`https://github.com/dcjrcjfc/infinite-canvas-aigc`
- 当前分支：`main`
- 当前提交：以 `main` 最新提交为准（本文件随每次迭代持续更新）
- 首读文件顺序：
  1. `README.md`
  2. `PROJECT_STATUS.md`
  3. `CHANGELOG.md`
  4. `项目说明.md`
