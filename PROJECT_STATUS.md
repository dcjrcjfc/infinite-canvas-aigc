# Project Status

最后更新：2026-04-26

## 1. 当前进度

### 已完成
- 项目已上传 GitHub：`main` 分支可用
- 三文件职责已明确并落实：
  - `无限画布/index.html`：仅结构与资源引用
  - `无限画布/style.css`：仅视觉样式与动效
  - `无限画布/script.js`：仅交互和业务逻辑
- 已实现核心功能：
  - 无限画布平移、缩放、节点拖拽
  - SVG 连线与节点删除联动清理
  - 文生图/图生图请求
  - 参考图自动流转（连线后上游图传递下游）
  - 参考图手动添加与移除
- 已做一轮交互流畅度优化：
  - 鼠标锚点缩放
  - 平移惯性
  - 基于 `requestAnimationFrame` 的重绘调度
  - 节点/按钮/连线细节动效
- 已完成一轮画布外层 UI 丰富化（参考资料视觉方向）：
  - 左上品牌 Logo 区
  - 右侧 Agent 助手侧栏（支持收起/展开）
  - 左下画布控制条（缩放滑杆、放大/缩小、重置、百分比）
  - 画布滚轮在侧栏与控制条区域做了防误触处理
- 已完成左侧工具栏升级与可用性修复：
  - 左侧由单按钮升级为多功能工具栏（添加/居中/重置/Agent）
  - 按钮支持提示气泡与激活态反馈
  - 顶部加入操作胶囊区，整体布局更接近参考风格
- 已完成缩放清晰度优化：
  - 画布位移对齐设备像素，缩放比例做精度收敛
  - 缩放渲染优化，降低放大时的模糊感
- 已完成模型切换能力升级：
  - 节点模型从“单一按钮”升级为“下拉列表可选”（当前含 `NanoBanana` / `gpt-image-2`）
  - 不同模型支持使用不同 API Key 路由
- 已完成生成流程调整：
  - 生成结果改为同节点就地更新（不再自动新建结果节点）
  - 上游节点更新后可同步刷新下游参考图
- 已完成滚轮/缩放交互修正：
  - 普通滚轮改为画布平移，`Ctrl + 滚轮` 才缩放
  - 修复缩放中心漂移，避免“放大后目标跑丢”
- 已完成右侧 Agent 交互改版：
  - 面板风格升级
  - 点击右上角关闭后，右下角显示小球入口；点击小球可恢复面板
- 已完成空画布引导层：
  - 无节点时显示中间引导内容
  - 有节点时自动隐藏；删空后重新显示
- 已完成一轮字号收敛：
  - 缩小空状态与 Agent 区域文字，提升信息密度与视觉平衡
- 已完成加号交互重构：
  - 上传素材节点改为轻量结构（仅图片框 + 左右加号）
  - 加号短按弹菜单、长按进入连线模式
  - 菜单建节点支持自动连线并继承上游参考图
- 已完成第一批可落地功能补全：
  - 历史面板支持本地存储/渲染/清空
  - 图片编辑器支持亮度/对比度/饱和度应用
  - Agent 快捷动作与发送可落到节点或创建文本节点
  - 顶部三按钮接入实际动作
  - 全局 toast 反馈接入关键动作
- 已完成第二批可落地功能补全：
  - 历史记录项可点击定位到对应节点（自动居中 + 选中）
  - 图片编辑器新增旋转90°与镜像，并可与滤镜叠加
  - 个人信息菜单项接入实际动作（通知/主页/账户/教程/登出反馈）
  - Agent 顶部按钮接入“新建文本节点”“打开历史”

### 进行中
- UI 细节仍在打磨阶段（目标参考 TapNow / 当前参考图风格）
- API 调用安全性待提升（当前前端明文 key）

### 阻塞/风险
- 网络偶发不稳定会影响远程推送与接口请求
- 前端明文 key 有泄露风险，不适合长期生产使用
- API 平台分组渠道需与模型权限一致，否则会报“无可用通道”

## 2. 下一步（优先级）

1. 第三批落地：编辑与历史进入“生产级”
   - 增加 `Undo / Redo`（节点增删、连线增删、图片编辑参数变更）
   - 历史支持按类型筛选与快速回放（定位 + 还原）
2. 节点系统增强
   - 节点重命名、复制/粘贴、批量选择
   - 节点类型图标与状态标签完善
3. 参考图工作流增强
   - 主参考图标记、权重控制、顺序调整
   - 引用来源可视化（当前参考图来自哪条链路）
4. 安全整改（上线前必须完成）
   - API key 改后端代理，移除前端明文密钥

## 3. 开发约束（必须遵守）

- `index.html` 只写 DOM 结构，不写内联 CSS/JS
- `style.css` 只写样式，不写行为逻辑
- `script.js` 只写逻辑，不写内联样式字符串
- 每次改动都需要更新 `CHANGELOG.md`（包含“原因”）

## 4. 交接给新 AI 的启动信息

- 仓库：`https://github.com/dcjrcjfc/infinite-canvas-aigc`
- 当前分支：`main`
- 当前提交：以 `main` 最新提交为准（本文件随每次迭代持续更新）
- 首读文件顺序：
  1. `README.md`
  2. `PROJECT_STATUS.md`
  3. `CHANGELOG.md`
  4. `项目说明.md`
